Int a, b, c

Int[100][3] arr

def max :: Int,Int -> Int
imp max (d, e) as
    if d > e then
        return d
    else
        Int f
        return e
    end
end

Int f, g

Record Punto as
      x :: Int
    , y :: Int
end

Record Mapa as
    coord :: Punto[10][10]
end

Record Wrapper as
    x :: Punto,
    y :: Punto
end

Record Deep as
    x :: Wrapper,
    y :: Wrapper
end


